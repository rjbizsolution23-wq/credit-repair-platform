import { Routes } from '@angular/router';

export const AI_ROUTES: Routes = [
  {
    path: '',
    children: [
      // All AI components are missing - no routes available
      // Removed ai-overview route - component not found
      // Removed dispute-analysis route - component not found
      // Removed letter-generation route - component not found

    ]
  }
];

export default AI_ROUTES;